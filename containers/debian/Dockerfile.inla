# SPDX-License-Identifier: GPL-3.0-only
# SPDX-FileCopyrightText: Copyright 2026 Jonas I. Liechti <j-i-l@t4d.ch>

# Layer 3/5: INLA.
#
# Installs INLA on top of the jags layer using the shared install_inla.sh
# script (stable -> testing fallback).

ARG BASE_IMAGE
FROM ${BASE_IMAGE}

LABEL org.opencontainers.image.licenses="GPL-3.0-only" \
      org.opencontainers.image.authors="Jonas I. Liechti <j-i-l@t4d.ch>"

COPY shared/install_inla.sh /tmp/install_inla.sh
RUN chmod +x /tmp/install_inla.sh && /tmp/install_inla.sh
